services:
  site:
    build:
      context: .
      dockerfile: Dockerfile.site
    image: ${SITE_IMAGE:-vpn_entrepreneur-site:latest}
    restart: unless-stopped
    ports:
      - "${SITE_PORT:-8080}:80"

